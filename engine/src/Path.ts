module Anuto {

    export class Path {

        public static init(): void {
            //
        }

        public static getPosition(l: number): {x: number, y: number} {

            let pos = {x: 0, y: 0};

            return pos;
        }
    }
}
